package net.dwarfguide.model.core;

/*Generated by MPS */

import java.util.Map;

public class CreatureStatistic {
  private Map<AttributeEnum, StatisticsTuple> stats;
  private RaceEnum race;

  public CreatureStatistic(RaceEnum race, Map<AttributeEnum, StatisticsTuple> stats) {
    this.race = race;
    this.stats = stats;
  }

  public int getAverage(AttributeEnum attr) {
    return (int) stats.get(attr).avg();
  }

  public int getMin(AttributeEnum attr) {
    return (int) stats.get(attr).min();
  }

  public int getMax(AttributeEnum attr) {
    return (int) stats.get(attr).max();
  }

  public int normalizeValue(int value, int maxValue, AttributeEnum attr) {
    return (value - getMin(attr)) * (maxValue / (getMax(attr) - getMin(attr)));
  }

  public RaceEnum getRace() {
    return race;
  }
}
