package sandbox.df2001;

/*Generated by MPS */

import java.util.Map;
import jetbrains.mps.internal.collections.runtime.MapSequence;
import java.util.LinkedHashMap;

public class EnumRegistry<T extends Enum<T> & HasName> {
  private final Map<String, T> map = MapSequence.fromMap(new LinkedHashMap<String, T>(16, (float) 0.75, false));

  public EnumRegistry() {
  }

  public void addEnum(T enumeration) {
    MapSequence.fromMap(map).put(enumeration.name(), enumeration);
  }

  public T findByName(String name) {
    return MapSequence.fromMap(map).get(name);
  }
}
